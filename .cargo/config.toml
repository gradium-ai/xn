# `getrandom` needs to be told which entropy source to use on wasm; there is no
# OS one. Applies only to the wasm target so native builds are unaffected.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
